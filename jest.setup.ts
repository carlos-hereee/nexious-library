// Registers @testing-library/jest-dom custom matchers (toBeInTheDocument,
// toHaveAttribute, toHaveAccessibleName, etc.) for every test via the jest
// setupFilesAfterEnv hook configured in package.json.
import "@testing-library/jest-dom";
