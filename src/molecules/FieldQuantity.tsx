// // import { getIn, useFormik } from "formik";
// // import { labels } from "../../atoms/forms/labels";
// import { InputQuantity, Label } from "src/atoms";
// import FieldQuantity from "./forms/FieldQuantity";

// const Form = ({ data, schema, max, min, change }) => {
//   // const { handleSubmit, handleBlur, handleChange, values, errors } = useFormik({
//   //   initialValues: data,
//   //   validationSchema: schema,
//   // });
//   // const handleOnChange = (e) => {
//   //   // handleChange(e);
//   //   change(e);
//   // };

//   return (
//     <form className="form" onSubmit={handleSubmit}>
//       {Object.keys(data).map((v) => (
//         <FieldQuantity name={v} key={v} />
//       ))}
//     </form>
//   );
// };

// export default Form;
